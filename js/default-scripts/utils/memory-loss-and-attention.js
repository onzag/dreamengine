/**
 * By default characters have perfect remembrance of everything they have seen and experienced.
 * This script modifies that by adding memory loss and attention span mechanics.
 * 
 * It will go through the items and characters that a character has seen, and randomly forget
 * some of them based on their attention span and memory retention settings.
 * 
 * You could even simulate ADHD or other cognitive conditions by tweaking these settings.
 * 
 * TODO this needs the RAG system to be fully functional, as it relies on character memories
 * being stored externally and fetched when needed.
 */